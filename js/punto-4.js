// suma de enteros
const prinum = prompt ('escriba el primero numero');
const segnum = prompt ('escriba el segundo numero');
const suma = 0;
suma = prinum + segnum;
console.log(`la suma de los numeros es ${suma}`);



const num1 = prompt('Ingrese un número por favor');
const num2 = prompt('Ingrese un número por favor');

const resultado = parseFloat(num1) + parseFloat(num2);

alert(`La suma es: ${resultado.toFixed(2)}`);